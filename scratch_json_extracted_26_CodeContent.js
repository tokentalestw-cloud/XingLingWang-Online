"with open(r'c:\\Users\\a2132\\Documents\\星靈王\\XingLingWang_v7_fixed\\static\\game_v8.js', 'r', encoding='utf-8') as f:\n    content = f.read()\n\n# Check that the function triggerSneakAttackSuccessEffects is defined exactly once and we can locate it.\ncount = content.count(\"async function triggerSneakAttackSuccessEffects\")\nprint(f\"Count of triggerSneakAttackSuccessEffects: {count}\")\n\n# Print the lines around it\nlines = content.splitlines()\nfound = -1\nfor i, line in enumerate(lines):\n    if \"async function triggerSneakAttackSuccessEffects\" in line:\n        found = i\n        break\n\nif found >= 0:\n    print(\"Found at line:\", found + 1)\n    for j in range(max(0, found - 2), min(len(lines), found + 15)):\n        print(f\"{j+1}: {lines[j]}\")\nelse:\n    print(\"NOT FOUND!\")\n"